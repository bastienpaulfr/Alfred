# Alfred

## Scripts

### system/update-packages.sh

Update Homebrew and other package managers.

**Usage:**
```bash
./system/update-packages.sh
```

**Features:**
- Updates Homebrew packages (`brew update && brew upgrade`)
- Easy to extend with other package managers (npm, pip, cargo, etc.)
