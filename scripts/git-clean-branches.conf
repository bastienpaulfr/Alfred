# git-clean-branches whitelist configuration
# One pattern per line. Supports glob wildcards (*, ?).
# Lines starting with # are comments. Empty lines are ignored.
# Branches matching any pattern here will NOT be deleted.

poc/*
keep/*
backup/*
